"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.convertFigmaClipboardToSvgHandler = void 0;
const figma_clipboard_1 = require("../../figma/figma-clipboard");
const convertFigmaClipboardToSvgHandler = ({ input }) => {
    try {
        const { svg } = (0, figma_clipboard_1.convertFigmaClipboardToSvg)(input.html);
        return Promise.resolve({ success: true, svg });
    }
    catch (error) {
        const reason = error instanceof Error ? error.message : String(error);
        return Promise.resolve({
            reason: reason.replace(/^Cannot import Figma selection:\s*/i, ''),
            success: false,
        });
    }
};
exports.convertFigmaClipboardToSvgHandler = convertFigmaClipboardToSvgHandler;
