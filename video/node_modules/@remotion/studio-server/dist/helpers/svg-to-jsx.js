"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.svgMarkupToJsx = void 0;
const core_1 = require("@svgr/core");
const plugin_jsx_1 = __importDefault(require("@svgr/plugin-jsx"));
const recast = __importStar(require("recast"));
const parse_ast_1 = require("../codemods/parse-ast");
const svgMarkupToJsx = async (markup) => {
    const componentCode = await (0, core_1.transform)(markup, {
        expandProps: false,
        jsxRuntime: 'automatic',
        plugins: [plugin_jsx_1.default],
        prettier: false,
        typescript: true,
    }, { componentName: 'PastedSvg' });
    const ast = (0, parse_ast_1.parseAst)(componentCode);
    let svgElement = null;
    recast.types.visit(ast, {
        visitJSXElement(path) {
            const { name } = path.node.openingElement;
            if (name.type === 'JSXIdentifier' && name.name === 'svg') {
                svgElement = path.node;
                return false;
            }
            this.traverse(path);
            return undefined;
        },
    });
    if (svgElement === null) {
        throw new Error('The pasted markup does not contain an <svg> root element');
    }
    return svgElement;
};
exports.svgMarkupToJsx = svgMarkupToJsx;
